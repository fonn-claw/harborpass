---
phase: 03-guest-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/sheet.tsx
  - src/components/ui/popover.tsx
  - src/lib/queries.ts
  - src/lib/format.ts
  - src/app/board/actions.ts
  - src/components/board/guest-detail-panel.tsx
  - src/components/board/amenity-logger.tsx
  - src/components/board/dispatch-board.tsx
autonomous: true
requirements: [AMEN-01, AMEN-02, AMEN-03, AMEN-04, AMEN-05, AMEN-06, AMEN-07]

must_haves:
  truths:
    - "Staff can open a slide-over panel from any checked-in or departing guest card showing stay details"
    - "Staff can log shower, fuel, shore power, pump-out, and laundry with single-tap actions"
    - "Shower logging respects included tokens (free if remaining, $3 if depleted)"
    - "Fuel logging prompts for gallons and type (diesel/gas) via popover"
    - "Shore power logging prompts for kWh amount via popover"
    - "Each amenity log creates a charge record and shows a toast confirmation"
    - "Guest card amenity badges update after logging"
  artifacts:
    - path: "src/components/board/guest-detail-panel.tsx"
      provides: "Slide-over panel with stay info, credentials, amenity log, and quick-action buttons"
      min_lines: 80
    - path: "src/components/board/amenity-logger.tsx"
      provides: "Row of icon buttons for logging amenities with popover forms for fuel/power"
      min_lines: 60
    - path: "src/app/board/actions.ts"
      provides: "logAmenity server action handling all 5 amenity types"
      exports: ["logAmenity"]
    - path: "src/lib/queries.ts"
      provides: "getStayDetail query with guest, slip, amenityUsages, charges relations"
      exports: ["getStayDetail"]
    - path: "src/lib/format.ts"
      provides: "formatCurrency helper for cents-to-dollars display"
      exports: ["formatCurrency"]
  key_links:
    - from: "src/components/board/amenity-logger.tsx"
      to: "src/app/board/actions.ts"
      via: "logAmenity server action call"
      pattern: "logAmenity"
    - from: "src/components/board/dispatch-board.tsx"
      to: "src/components/board/guest-detail-panel.tsx"
      via: "detailStayId state controlling Sheet open"
      pattern: "detailStayId"
    - from: "src/components/board/guest-detail-panel.tsx"
      to: "src/lib/queries.ts"
      via: "stay data from BoardData props"
      pattern: "stayId"
---

<objective>
Build the guest detail slide-over panel with amenity logging quick actions. Staff can click any checked-in or departing guest card (or occupied dock strip slot) to see full stay details and log amenity usage with single-tap actions.

Purpose: This is the primary interaction for tracking amenity revenue during a guest's stay -- the core "during stay" workflow.
Output: Guest detail Sheet component, amenity logger with 5 amenity types, logAmenity server action, getStayDetail query, formatCurrency helper.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guest-lifecycle/03-CONTEXT.md
@.planning/phases/03-guest-lifecycle/03-RESEARCH.md

@src/db/schema.ts
@src/app/board/actions.ts
@src/lib/queries.ts
@src/components/board/dispatch-board.tsx
@src/components/board/guest-card.tsx
@DESIGN-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/queries.ts:
```typescript
export interface BoardData {
  arriving: StayWithRelations[];
  checkedIn: StayWithRelations[];
  departingToday: StayWithRelations[];
}
// StayWithRelations includes: guest, slip, amenityUsages relations
```

From src/db/schema.ts:
```typescript
// amenityUsage: stayId, type (amenity_type enum), quantity (decimal), unitPrice (int cents), totalAmount (int cents), fuelType (nullable)
// charges: stayId, description, category, amount (int cents)
// stays: showerTokens (default 3), showerTokensUsed (default 0), gateCode, wifiPassword, nightlyRate
// pricing: category, name, rate (int cents), unit
```

From src/lib/session.ts:
```typescript
export interface SessionData {
  userId: number;
  role: "staff" | "manager" | "guest";
  name: string;
}
```

From src/components/board/dispatch-board.tsx:
```typescript
type StayData = BoardData["arriving"][number];
// handleViewStay and handleSettle are placeholder callbacks that need to be wired
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components, add queries, server action, and formatCurrency helper</name>
  <files>
    src/components/ui/sheet.tsx,
    src/components/ui/popover.tsx,
    src/lib/queries.ts,
    src/lib/format.ts,
    src/app/board/actions.ts
  </files>
  <action>
1. Install shadcn Sheet and Popover: `npx shadcn@latest add sheet popover --yes`

2. Create `src/lib/format.ts` with a `formatCurrency` helper:
   - Input: cents (number), Output: formatted string like "$85.00"
   - `export function formatCurrency(cents: number): string { return "$" + (cents / 100).toFixed(2); }`

3. Add `getStayDetail` query to `src/lib/queries.ts`:
   - Takes stayId (number), returns stay with guest, slip, amenityUsages (ordered by createdAt desc), and charges (ordered by createdAt desc) using db.query.stays.findFirst with `with:` relations
   - Import `eq` from drizzle-orm, reference `stays` from schema

4. Add `logAmenity` server action to `src/app/board/actions.ts`:
   - Discriminated union input type: shower (stayId), fuel (stayId, gallons, fuelType), shore_power (stayId, kWh), pump_out (stayId), laundry (stayId)
   - Look up all pricing from `db.select().from(pricing)` once at start
   - Shower logic: query the stay, check showerTokensUsed vs showerTokens. If used < total: increment showerTokensUsed, create amenityUsage with totalAmount=0, do NOT create charge, message "Shower (included token)". If depleted: increment showerTokensUsed, look up "Shower Token" price from pricing (fallback 300), create amenityUsage with totalAmount=price, create charge record, message "Shower token (paid)"
   - Fuel: find pricing by name "Diesel" or "Gas" matching input.fuelType (fallback 549/429). quantity=gallons, totalAmount=round(unitPrice*quantity). Category "fuel". Create amenityUsage with fuelType set. Create charge.
   - Shore power: prompt provides kWh. Unit price = 12 cents/kWh (per CONTEXT.md locked decision, NOT the per-day rate from pricing table). totalAmount = round(12 * kWh). Category "amenity". Create amenityUsage. Create charge.
   - Pump-out: look up "Pump-out" from pricing (fallback 1500). Single tap, quantity=1. Create amenityUsage + charge.
   - Laundry: look up "Laundry Token" from pricing (fallback 500). Single tap, quantity=1. Create amenityUsage + charge.
   - Insert amenityUsage: { stayId, type, quantity: String(quantity), unitPrice, totalAmount, fuelType (null for non-fuel) }
   - Insert charge (when shouldCharge && totalAmount > 0): { stayId, description, category, amount: totalAmount }
   - Call revalidatePath("/board") after mutations
   - Return { success: boolean, message: string }
   - Wrap in try/catch, return { success: false, message: "Failed to log amenity" } on error
   - Import: amenityUsage, charges, pricing, stays from schema. eq from drizzle-orm. revalidatePath from next/cache.
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/harborpass && npm run build</automated>
  </verify>
  <done>shadcn Sheet and Popover installed, formatCurrency helper exists, getStayDetail query returns stay with all relations, logAmenity handles all 5 amenity types with correct pricing logic and shower token tracking</done>
</task>

<task type="auto">
  <name>Task 2: Build guest detail slide-over panel with amenity logger and wire to dispatch board</name>
  <files>
    src/components/board/guest-detail-panel.tsx,
    src/components/board/amenity-logger.tsx,
    src/components/board/dispatch-board.tsx
  </files>
  <action>
1. Create `src/components/board/amenity-logger.tsx` ("use client"):
   - Props: stayId (number), showerTokens (number), showerTokensUsed (number)
   - Row of 5 icon buttons: shower, fuel, shore_power, pump_out, laundry
   - Each button: icon from /assets/icon-{type}.svg (shower, fuel, power, pump-out, laundry), tooltip label via shadcn Tooltip
   - Single-tap amenities (shower, pump_out, laundry): call logAmenity directly, show toast with result.message via sonner
   - Fuel button: opens shadcn Popover with a small form — number input for gallons (step=0.1, min=0.1), select for fuel type (diesel/gas), "Log Fuel" submit button. On submit call logAmenity({ type: "fuel", stayId, gallons, fuelType }), close popover, toast.
   - Shore power button: opens shadcn Popover with number input for kWh (step=0.1, min=0.1), "Log Power" submit button. On submit call logAmenity({ type: "shore_power", stayId, kWh }), close popover, toast.
   - Show shower token count near shower button: "{used}/{total}" in small text
   - Use useTransition for pending states on each button (disable while pending)
   - Style: flex row, gap-2, each button is a 40x40 rounded-lg bg-sand hover:bg-fog/60 with centered 20x20 icon

2. Create `src/components/board/guest-detail-panel.tsx` ("use client"):
   - Props: stayId (number | null), stays (BoardData — all three arrays flattened to find the matching stay by id), open (boolean), onOpenChange (fn)
   - Uses shadcn Sheet (side="right"). Sheet width: className "w-[400px] sm:w-[400px]" on SheetContent, full-width on mobile via default Sheet behavior
   - Find the stay from the flattened stays array by stayId (no separate fetch — data already in BoardData props)
   - Content sections (top to bottom):
     a. SheetHeader: guest name (font-heading text-navy font-bold), vessel name, slip badge (slip.name in font-mono bg-ocean/10 text-ocean rounded px-2)
     b. Stay info section: check-in date (formatted with date-fns format "MMM d, yyyy"), expected departure, nights remaining (differenceInCalendarDays from date-fns). Use Separator between sections.
     c. Credentials section: gate code, Wi-Fi password, shower tokens (used/remaining) — all in font-mono text-lg with bg-navy/5 rounded-lg p-3 for high contrast. Icons from /assets/ (icon-gate.svg, icon-wifi.svg, icon-token.svg).
     d. Amenity log section: scrollable list (max-h-48 overflow-y-auto) of amenityUsages ordered by createdAt desc. Each row: icon for type, description, formatted amount (formatCurrency), timestamp (format "MMM d h:mm a"). If no usages, show "No amenity usage yet" empty state in text-slate.
     e. AmenityLogger component at bottom, passing stayId, showerTokens, showerTokensUsed

3. Modify `src/components/board/dispatch-board.tsx`:
   - Add state: `const [detailStayId, setDetailStayId] = useState<number | null>(null);`
   - Wire handleViewStay: `setDetailStayId(stay.id)` (replace toast placeholder)
   - Wire handleSlipClick for occupied/departing slips: find the stay from data arrays whose slip.id matches, set detailStayId (replace toast placeholder). Keep available slip logic unchanged.
   - Also allow departing cards' "Settle" button to still work separately (keep handleSettle as toast placeholder for now — Plan 02 will wire it)
   - Add onViewStay to Departing column so departing cards can also open the detail panel (add another callback or reuse)
   - Flatten all stays: `const allStays = [...data.arriving, ...data.checkedIn, ...data.departingToday]`
   - Render GuestDetailPanel: `<GuestDetailPanel stayId={detailStayId} stays={allStays} open={detailStayId !== null} onOpenChange={(open) => { if (!open) setDetailStayId(null); }} />`
   - Import GuestDetailPanel
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/harborpass && npm run build</automated>
  </verify>
  <done>Clicking "View Stay" on checked-in cards opens a right-side slide-over showing stay details, credentials, amenity log, and quick-action buttons. Clicking occupied dock strip slots opens the same panel. Single-tap amenities (shower, pump-out, laundry) log immediately with toast. Fuel and shore power open popovers for quantity input. Amenity badges on guest cards update after logging via revalidatePath.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no type errors
- Guest detail panel opens from checked-in card "View Stay" button
- Guest detail panel opens from occupied dock strip slot click
- Amenity logger shows 5 icon buttons with correct icons
- Single-tap shower logs usage and shows toast
- Fuel popover collects gallons + type and logs
- Shore power popover collects kWh and logs
- Amenity badges on cards update after logging
</verification>

<success_criteria>
Staff can open a guest detail slide-over from any checked-in or departing guest card, see full stay information with credentials, and log all 5 amenity types with appropriate UI (single-tap or popover input). Each log creates the correct charge record and the board updates.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guest-lifecycle/03-01-SUMMARY.md`
</output>
