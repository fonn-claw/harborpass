---
phase: 04-manager-views
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/manager/pricing-config.tsx
  - src/app/manager/actions.ts
autonomous: true
requirements: [MNGR-04]

must_haves:
  truths:
    - "Manager can see all pricing entries grouped by category (slip rates, amenity fees, fuel prices)"
    - "Manager can edit a price inline and save it"
    - "Toast confirmation appears after saving a price change"
    - "Values display as dollars and save as integer cents to DB"
  artifacts:
    - path: "src/app/manager/pricing-config.tsx"
      provides: "Client component with inline-editable pricing table"
      contains: "use client"
    - path: "src/app/manager/actions.ts"
      provides: "Server action to update pricing rate"
      exports: ["updatePricingRate"]
  key_links:
    - from: "src/app/manager/pricing-config.tsx"
      to: "src/app/manager/actions.ts"
      via: "Server action call on save"
      pattern: "updatePricingRate"
    - from: "src/app/manager/page.tsx"
      to: "src/app/manager/pricing-config.tsx"
      via: "Import and render with pricing data prop"
      pattern: "PricingConfig"
---

<objective>
Build the pricing configuration section where managers can view and inline-edit slip rates, amenity fees, and fuel prices.

Purpose: Lets the marina manager adjust pricing without developer intervention -- rates change seasonally and need quick updates.
Output: Interactive pricing editor with server action persistence and toast feedback.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-manager-views/04-CONTEXT.md
@src/db/schema.ts
@src/lib/format.ts
@src/app/manager/page.tsx
@src/app/manager/layout.tsx
@src/app/board/actions.ts
@DESIGN-SPEC.md

<interfaces>
<!-- Pricing table schema -->
From src/db/schema.ts:
```typescript
export const pricing = pgTable("pricing", {
  id: serial("id").primaryKey(),
  category: varchar("category", { length: 50 }).notNull(),
  name: varchar("name", { length: 100 }).notNull(),
  rate: integer("rate").notNull(), // cents
  unit: varchar("unit", { length: 20 }).notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

Seed pricing values:
- slip: Small Nightly (5500), Medium Nightly (8500), Large Nightly (12500) — unit: "night"
- amenity: Shower Token (300/use), Laundry Token (500/use), Pump-out (1500/service), Shore Power (1200/day)
- fuel: Diesel (549/gallon), Gas (429/gallon)

From src/lib/format.ts:
```typescript
export function formatCurrency(cents: number): string; // 8500 -> "$85.00"
```

Server action pattern from src/app/board/actions.ts:
```typescript
"use server";
import { db } from "@/db";
import { revalidatePath } from "next/cache";
// Uses sequential db operations, no transactions (Neon HTTP driver)
```

Existing toast setup: sonner is installed and Toaster component is mounted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server action and pricing query</name>
  <files>src/app/manager/actions.ts, src/lib/queries.ts</files>
  <action>
Create src/app/manager/actions.ts with "use server" directive.

`updatePricingRate(id: number, newRate: number)` server action:
- Validate: id must be a positive integer, newRate must be a non-negative integer (cents)
- Update: `db.update(pricing).set({ rate: newRate, updatedAt: new Date() }).where(eq(pricing.id, id))`
- Call `revalidatePath("/manager")` after update
- Return `{ success: true }` or `{ success: false, error: string }`
- Follow the pattern from src/app/board/actions.ts (sequential operations, no transactions)

Add `getAllPricing()` to src/lib/queries.ts:
- Query: `db.select().from(pricing).orderBy(pricing.category, pricing.name)`
- Return all pricing rows
- Export the return type as `PricingEntry`
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/harborpass && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Server action compiles, query function exported, types correct.</done>
</task>

<task type="auto">
  <name>Task 2: Pricing config client component and wire into manager page</name>
  <files>src/app/manager/pricing-config.tsx, src/app/manager/page.tsx</files>
  <action>
Create src/app/manager/pricing-config.tsx as a "use client" component.

Props: `{ pricing: PricingEntry[] }` (type from queries.ts).

**Layout:**
- Card wrapper (white bg, rounded-lg, shadow-sm, p-6) matching other manager sections
- Title: "Pricing Configuration" with icon-settings.svg icon
- Group pricing entries by category with section subheadings:
  - "Nightly Slip Rates" for category=slip
  - "Amenity Fees" for category=amenity
  - "Fuel Prices" for category=fuel

**Each pricing row:**
- Name (e.g. "Small Nightly"), unit label (e.g. "per night"), and the editable rate
- Rate displays as dollars (formatCurrency) in a normal state
- Click the rate to enter edit mode: replace text with an Input (type="number", step="0.01") pre-filled with the dollar value (rate / 100)
- Show a small "Save" button (Button component, size="sm", variant ocean bg) and "Cancel" text button next to the input
- On Save: convert dollar input back to cents (Math.round(parseFloat(value) * 100)), call updatePricingRate server action, show toast("Price updated") on success via sonner, exit edit mode
- On Cancel: revert to display mode, discard changes
- Track editing state with useState: `editingId: number | null` and `editValue: string`

**Validation:**
- Input must be a valid positive number
- If invalid on save, show toast.error("Invalid price")
- Disable save button while value is empty or non-numeric

**Styling:**
- Rows: flex justify-between items-center, py-3, border-b border-fog last:border-0
- Category section headings: text-sm font-semibold text-slate uppercase tracking-wide mb-2 mt-4 (first mt-0)
- Rate text in display mode: font-mono text-navy font-semibold
- Edit input: w-24 text-right
- Use Separator between category groups

**Wire into page.tsx:**
- Import getAllPricing in the manager page Server Component
- Fetch pricing data alongside existing queries
- Import and render PricingConfig below the GuestHistory section, passing pricing as prop
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/harborpass && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Pricing config renders all 9 entries grouped by category. Inline edit works: clicking rate shows input, Save persists to DB, toast confirms. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Manager page at /manager shows pricing config section below guest history
- All 9 pricing entries visible grouped into 3 categories
- Clicking a rate opens inline editor with correct dollar value
- Saving a changed rate persists to database and shows toast
- Canceling edit reverts to original value
- Values correctly convert between cents (DB) and dollars (display)
</verification>

<success_criteria>
- Manager sees all pricing entries organized by category (slip rates, amenity fees, fuel prices)
- Manager can click any rate to edit it inline
- Saving updates the database and confirms with a toast
- Dollar/cent conversion is correct in both directions
</success_criteria>

<output>
After completion, create `.planning/phases/04-manager-views/04-02-SUMMARY.md`
</output>
